{
  "name": "discord-bot",
  "version": "1.0.0",
  "description": "A simple Discord bot that greets users.",
  "main": "bot.js",  // Change this to your file name if using something else
  "scripts": {
    "start": "node bot.js"  // Change this to your file name if using something else
  },
  "dependencies": {
    "discord.js": "^14.0.0",  // Use the latest version
    "moment": "^2.29.1"  // Use the latest version
  },
  "engines": {
    "node": ">=16.0.0"  // Specify the Node.js version
  },
  "license": "MIT"
}
